import { View, Text } from "react-native";
import React from "react";
import { IconButton } from "react-native-paper";

export default function index({ icon, iconColor, size, onPress }) {
  console.log("NO USAR");
  return (
    <View>
      {/* <IconButton icon="camera" iconColor size onPress={onPress} /> */}
    </View>
  );
}
